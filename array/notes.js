const notes = ["note 1", "note 2", "note 3"]; // we can as many as we want // not needed to be of same data type
console.log(notes);
console.log(notes.length); // remember length is property so no ()
console.log(notes[notes.length - 1]); // index start at 0;
